# MQTTBroker
A lightweight MQTT broker designed to enable communication between multiple components using the publish/subscribe pattern.
It supports MQTT protocol versions 3.1 and 5.0, and allows connections over TCP, TLS, and WebSocket.
